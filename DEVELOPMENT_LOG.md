# 介護記録システム 開発ログ

老健施設向けAI活用・ツール作成の検討から、スプレッドシート連携Webアプリの完成までの記録。

---

## 1. 老健施設へのAI導入検討

### 前提条件
- AIを使えるのは限られた職員（1〜2名）
- 現状はアナログ業務が多い
- 人手不足・経営難

### AI活用の方向性
「AIを使える職員が作ったツールを、他の職員が使うだけ」という構造が現実的。

### 具体的な活用領域

#### 記録・書類の自動化
| ツール | 概要 | コスト |
|---|---|---|
| 音声入力→介護記録 | 話すだけでテキスト化・記録 | 月〜2千円 |
| 申し送り自動要約Bot | 長い申し送りを3点に整理 | 月〜千円 |
| 施設サービス計画書ドラフト生成 | 入力内容から文章を自動生成 | 月〜2千円 |
| ヒヤリハット報告書自動作成 | 簡単な入力から書式に変換 | ほぼ無料 |

#### 情報共有・ケア支援
| ツール | 概要 | コスト |
|---|---|---|
| 家族向けFAQ自動応答Bot | LINE Worksで24時間即答 | 月〜5千円 |
| バイタル異常アラート通知 | 閾値超えをLINE/Slackに通知 | ほぼ無料 |
| シフト希望→最適シフト自動作成 | GASで叩き台を自動生成 | ほぼ無料 |

#### 経営・収益の改善
| ツール | 概要 | コスト |
|---|---|---|
| 介護報酬 加算算定チェック | 取り漏れを自動検出 | 完全無料 |
| 稼働率・収益ダッシュボード | Looker Studioで自動生成 | ほぼ無料 |
| 補助金・助成金情報収集Bot | 申請機会の見逃しを防止 | 月〜千円 |

#### 研修・教育支援
| ツール | 概要 | コスト |
|---|---|---|
| 新人職員向けQ&AチャットBot | マニュアルを読み込んだRAG構成 | 月〜2千円 |
| eラーニング小テスト自動生成 | 研修資料から問題を自動作成 | ほぼ無料 |

---

## 2. NotebookLM用プロンプト集

老健施設AI導入の資料作成用プロンプト10本を作成。

### カテゴリ構成
1. **資料の骨格を作る** — 目次・構成案、エグゼクティブサマリー
2. **課題・現状の整理** — 課題の言語化、アナログ業務の時間コスト可視化
3. **AI活用の具体策** — 優先度順ツール一覧、低コストツール選定
4. **経営への説得材料** — 加算取り漏れ防止、ROI試算
5. **職員への説明・研修用** — FAQ作成、3ヶ月導入ロードマップ

### 使い方
1. NotebookLMに関連資料（厚労省通知・施設マニュアル等）をアップロード
2. プロンプトをコピーしてチャット欄に貼り付ける
3. 推奨順序：01→02→03→04→05

---

## 3. SaaS設計と複合管理ソフトの方針

### 技術スタック検討
- フロント: Next.js + Tailwind CSS（Vercel無料デプロイ）
- 認証: NextAuth.js
- AI: Claude / OpenAI API
- DB: Supabase
- 決済（将来）: Stripe

### 最終的な選択
**Googleスプレッドシート連携**に決定。
- 利用者ごとに1ブック作成
- Google Apps Script（GAS）経由で自動書き込み
- 初期コストゼロ・施設側の学習コスト最小

---

## 4. Webアプリの仕様変遷

### v1〜v4：レイアウト模索期

| バージョン | 変更内容 |
|---|---|
| v1 | 音声入力→介護記録の基本設計 |
| v2 | 利用者名簿一覧トップ、食事インライン入力、看護記録タブ |
| v3 | アコーディオン廃止→常時展開 |
| v4 | 左ペイン名前一覧＋右ペイン入力の分割レイアウト |

### v5〜v6：表形式への転換

**v5 — 表形式の確立**
- 名前｜主食｜副食｜水分｜特記 の表構成
- タップで摂取量がサイクル切替（全量→8割→6割→4割→2割→なし）
- 水分は100ml刻みでタップサイクル

**v6 — タブレット対応**
```
スマホ（〜767px）   : コンパクト表示
タブレット（768px〜）: ボタン52px・文字14px・列幅拡大
PC（1024px〜）      : ボタン58px・文字15〜16px
```

### v7〜v9：機能追加期

**v7 — 欠食ボタン追加**
- タップで主食・副食・汁物が全部「なし」・水分0mlに
- 欠食中は摂取量ボタンが無効化（薄く表示）
- 再タップで全量・800mlに復帰

**v8 — 排泄記録の強化**
- 排泄に「浣腸」「下剤」チップを追加（複数選択可）
- 排尿・排便それぞれの回数カウンター（－／＋ボタン）

**v9 — 汁物削除・排泄シンプル化**
- 汁物列を削除（主食・副食の2列に）
- 排泄を「排尿回数・排便回数・浣腸下剤チップ」のみに整理

### v10〜v12：朝昼夕と排泄タブ独立

**v10 — 朝昼夕サブタブ**
- 介護記録タブ内に「朝食・昼食・夕食」のサブタブを追加
- 朝・昼・夕それぞれでデータが独立管理される
- 全区分保存済みになるとタブが緑の「完了」スタイルに変化

**v11 — 排泄タブの独立**
- タブ構成：介護記録｜排泄記録｜看護記録 の3タブに
- 体温デフォルト36.5℃、SpO2デフォルト98%
- 看護記録から排泄列を完全分離

**v12 — 排泄の時刻ログ化**
- 排尿・排便ごとに「時刻（1h単位）」を記録
- ＋排尿 / ＋排便 ボタンで行追加、×で削除
- 現在時刻をデフォルトに自動セット
- 保存時：「08:00 排尿、10:00 排便」のようにスプレッドシートに記録

### v13〜v15：仕上げ

**v13 — 便の状態選択**
- 排便ログに状態セレクトを追加
- 選択肢：普通便・軟便・硬便・水様便・泥状便
- 保存時：「10:00 排便（軟便）」のように記録

**v14 — 血圧レイアウト修正**
- 血圧の上・下を縦並びに変更（横並びで見切れる問題を解消）
- 専用列幅（スマホ70px・タブレット90px・PC 100px）

**v15 — GAS連携完成**
- `postGAS()` にエラーハンドリング・residentName自動付与を追加
- GAS未設定時はデモモードで動作（モック保存）

---

## 5. 最終仕様

### 画面構成

```
[ヘッダー]  介護記録  日付  全員保存
[タブバー]  🍚介護記録 | 🚽排泄記録 | 🩺看護記録
[朝昼夕バー] 🌅朝食 | ☀️昼食 | 🌙夕食  ← 介護記録タブのみ表示
[テーブル]  利用者一覧（sticky左カラム）+ 入力セル群
[フッター]  ○/8名 保存済  ⚠ アラート○名
```

### 介護記録タブ（食事・水分）

| 列 | 内容 |
|---|---|
| 利用者名 | 固定（sticky） |
| 主食 | タップでサイクル（全量→8割→6割→4割→2割→なし） |
| 副食 | 同上 |
| 水分 | タップで100ml刻みサイクル（1000→0→1000…） |
| 欠食 | タップでON→全項目なし・水分0ml／再タップで全量復帰 |
| 特記 | テキスト入力 |
| 保存 | 1名ずつ保存／全員一括保存あり |

### 排泄記録タブ

| 列 | 内容 |
|---|---|
| 利用者名 | 固定 |
| 排泄ログ | ＋排尿 / ＋排便 で行追加・時刻セレクト（1h単位）・便の状態（排便のみ）・×で削除 |
| 浣腸・下剤 | タップでオン・オフ（複数選択可） |
| 特記 | テキスト入力 |
| 保存 | — |

**便の状態選択肢**：普通便・軟便・硬便・水様便・泥状便

### 看護記録タブ

| 列 | 内容 |
|---|---|
| 利用者名 | 固定 |
| 体温 | 数値入力（デフォルト36.5℃）・37.5℃以上でアラート |
| 血圧 | 上・下を縦並び入力（上160以上でアラート） |
| SpO2 | 数値入力（デフォルト98%）・95%未満でアラート |
| 脈拍 | 数値入力 |
| 観察 | チップ選択（良眠・覚醒・傾眠・不穏・発熱・疼痛） |
| 特記 | テキスト入力 |
| 保存 | — |

---

## 6. GAS連携仕様

### アーキテクチャ

```
kaigo_kiroku.html
    ↓ fetch POST（JSON）
Google Apps Script（ウェブアプリとしてデプロイ）
    ↓
Googleドライブ / 指定フォルダ
    └ [利用者名]_介護記録.xlsx
        ├ 食事・水分シート
        ├ 排泄記録シート
        └ 看護記録シート
```

### スプレッドシートのシート構成

**食事・水分シート**
```
記録日時 | 利用者名 | 部屋 | 食事区分 | 摂取内容 | 水分量 | 特記事項
```

**排泄記録シート**
```
記録日時 | 利用者名 | 部屋 | 回数集計 | 時刻別ログ | 浣腸・下剤 | 特記事項
```

**看護記録シート**
```
記録日時 | 利用者名 | 部屋 | バイタル | 観察所見 | 特記事項
```

### GASセットアップ手順

1. `script.google.com` → 新しいプロジェクト作成
2. `kaigo_gas.js` の内容を貼り付け
3. `FOLDER_ID` を保存先フォルダのIDに変更
4. `testPing()` を実行して動作確認
5. デプロイ → ウェブアプリ → 実行ユーザー：自分 → アクセス：全員
6. 発行されたURLを `kaigo_kiroku.html` の `GAS_URL` に貼り付け

### フォルダIDの確認方法

```
https://drive.google.com/drive/folders/【ここがFOLDER_ID】
```

---

## 7. ファイル一覧

| ファイル名 | 役割 |
|---|---|
| `kaigo_kiroku.html` | Webアプリ本体（HTML単体・サーバー不要） |
| `kaigo_gas.js` | Google Apps Script（スプレッドシート連携） |
| `README.md` | セットアップ手順 |
| `DEVELOPMENT_LOG.md` | 本ファイル（開発ログ） |

---

## 8. 今後の拡張アイデア

- [ ] 利用者マスタをGASから動的に取得（現在はHTMLにハードコード）
- [ ] 申し送り自動要約（Claude API連携）
- [ ] バイタル推移グラフの自動生成（Looker Studio連携）
- [ ] 家族向けFAQ Bot（LINE Works連携）
- [ ] 加算算定チェック機能の追加
- [ ] PWA化（ホーム画面に追加してアプリのように使用）
- [ ] オフライン対応（Service Worker）
